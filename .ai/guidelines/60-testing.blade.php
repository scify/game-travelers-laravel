## Testing

- Every change must be tested. Write or update an existing test, then run affected tests.
- Conventions (structure, naming, factories, mocking, hermeticity) live in `tests/CLAUDE.md`. Read it before writing tests.
- Run minimum tests needed: `--filter=method_name` or specific file paths.
