# DDEV configuration for local development.
# Reference: https://ddev.readthedocs.io/en/stable/users/configuration/config/
name: travelers
type: laravel
docroot: public
php_version: "8.4"
webserver_type: nginx-fpm
xdebug_enabled: false
additional_hostnames: []
additional_fqdns: []
database:
    type: mysql
    version: "8.0"
# Uploaded files live in storage/, outside the web root.
upload_dirs:
    - storage
use_dns_when_possible: true
timezone: Europe/Athens
composer_version: "2"
web_environment: []
# Node.js version. Must match .nvmrc; the post-start hook below keeps them in step.
nodejs_version: "24.21.0"
corepack_enable: false
# Vite's development server, reached by the browser through the DDEV router.
# The https port is VITE_DEV_PORT in .env (see vite.config.js); each project
# on the same machine needs its own pair.
web_extra_exposed_ports:
    - name: vite
      container_port: 5151
      http_port: 5150
      https_port: 5151
ddev_version_constraint: ">= 1.25.3"
hooks:
    post-start:
        - exec: bash .ddev/scripts/sync-node-version.sh
